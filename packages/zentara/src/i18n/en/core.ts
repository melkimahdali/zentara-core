import type { Messages } from "../id/index.js";

export const core: Messages["core"] = {
  passwordEmpty: "Password must not be empty",
  passwordTooLong: (max) => `Password must be at most ${max} characters`,
  loginRequired: "Please sign in first",
  sessionExpired: "Your session is no longer valid, please sign in again",
  forbidden: "You do not have access to this feature",
  invalidPort: (value) => `Invalid port: ${value} (must be an integer 0-65535)`,
  configExport: (name) => `${name} must export a config object as default`,
  sessionMissing: "ctx.session is not available: add the session() middleware first",
  cookieName: (name) => `Invalid cookie name: ${name}`,
  cookiePath: (path) => `Invalid cookie path: ${path}`,
  cookieDomain: (domain) => `Invalid cookie domain: ${domain}`,
  cookieMaxAge: "Cookie maxAge must be a number",
  cookieSent: (name) => `Cannot set cookie "${name}": response headers were already sent`,
  nextTwice: "next() was called more than once in one middleware",
  pluginInvalid: (name) => `Invalid plugin: ${name}`,
  rateLimitOptions: "rateLimit(): windowMs and max must be positive",
  tooManyAttempts: "Too many attempts, please try again later",
  segmentInvalid: (raw, file) => `Invalid route segment "${raw}" in ${file}`,
  paramInvalid: (name, file) => `Invalid parameter name "${name}" in ${file}`,
  catchAllLast: (file) => `A catch-all must be the last segment: ${file}`,
  paramDuplicate: (file) => `Duplicate parameter name in ${file}`,
  middlewareArray: (file) => `The "middleware" export in ${file} must be an array of functions`,
  exportFunction: (key, file) => `The "${key}" export in ${file} must be a function`,
  noHandler: (file) => `Route file ${file} does not export a handler (default, GET, POST, ...)`,
  routesDirMissing: (dir) => `Routes folder not found: ${dir}`,
  routeClash: (a, b, pattern) => `Route clash: ${a} and ${b} both map to ${pattern}`,
  useAfterStart: "runtime.use() must be called before start()",
  useFunction: "runtime.use() only accepts middleware functions",
  middlewareFile: (file) => `${file} must export an array of middleware as default`,
  middlewareLoaded: (n, file) => `Loaded ${n} middleware from ${file}`,
  errorPageFailed: "Failed to render the error page",
  csrf: "Cross-origin request rejected (CSRF protection)",
  sessionSecretRequired: "session(): a secret is required in production (set the SESSION_SECRET env)",
  sessionSecretShort: (min) => `session(): the secret must be at least ${min} characters`,
  sessionRandomSecret: "session(): SESSION_SECRET is not set; using a random secret (sessions are lost on restart)",
  sessionNotSaved: (path) => `session(): session changes on ${path} were not saved because the response was already sent`,
  sessionTooLarge: (size, max) => `Session too large (${size} bytes, limit ${max}); store large data in the database`,
  validationFailed: "Validation failed",
  attrInvalid: (name) => `Invalid attribute name: ${name}`,
  tagInvalid: (name) => `Invalid tag name: ${name}`,
};
