import type { Messages } from "../id/index.js";

export const backend: Messages["backend"] = {
  jobNoHandler: (file) => `${file} must export a job function as default`,
  jobBadRetries: (name) => `Job ${name}: \`retries\` must be an integer of 0 or more`,
  jobBadSchedule: (name) => `Job ${name}: \`schedule\` must be a cron string, e.g. "0 7 * * *"`,
  jobUnknown: (name, known) => `Unknown job: ${name}. Available jobs: ${known}`,
  scheduleFailed: (name) => `Failed to schedule job ${name}`,
  queueError: "Job queue error",
  jobDone: (name, ms) => `Job ${name} finished (${ms} ms)`,
  jobRetry: (name, attempt, max, seconds, error) => `Job ${name} failed (attempt ${attempt}/${max}), retrying in ${seconds} seconds: ${error}`,
  jobFailed: (name, attempts) => `Job ${name} failed after ${attempts} ${attempts === 1 ? "attempt" : "attempts"}`,
  mailHeaderInvalid: (name) => `Invalid email header "${name}" (must not contain line breaks)`,
  mailAddressInvalid: (value) => `Invalid email address: ${value}`,
  mailNoRecipient: "An email needs at least one recipient (to)",
  mailNoBody: "An email needs a body: text or html",
  mailBadUrl: (url) => `Invalid MAIL_URL: ${url} (use smtp://, smtps://, log, or memory)`,
  mailTimeout: (host) => `SMTP server ${host} did not respond`,
  mailSmtpError: (step, reply) => `SMTP failed at ${step}: ${reply}`,
  mailNotConfigured: "Email delivery is not set up. Set MAIL_URL (e.g. smtp://user:pass@smtp.example.com:587) in .env",
  mailNoFrom: "No sender address: set MAIL_FROM in .env or `from` on the message",
  mailLogged: (to, subject, file) => `✉ Email (not sent, log mode) to ${to}: "${subject}" · ${file}`,
  uploadBadForm: "The form could not be read",
  uploadEmpty: "The file is empty",
  uploadTooLarge: (mb) => `The file is too large (max ${mb} MB)`,
  uploadTypeNotAllowed: (ext) => `.${ext} files are not allowed`,
  uploadContentMismatch: (ext) => `The file content does not match its type (.${ext})`,
  cli: {
    noJobs: (dir) => `No jobs in ${dir} yet. Create one with: zusantara make:job <name>`,
    header: "JOB",
    retries: "RETRIES",
    schedule: "SCHEDULE",
    next: "NEXT RUN",
    queue: (queued, running, failed, done) => `Queue: ${queued} waiting · ${running} running · ${failed} failed · ${done} done`,
    runUsage: "Usage: zusantara jobs:run <name> [--data <json>]",
    badData: "--data must be valid JSON",
    ran: (name) => `✓ Job ${name} finished.`,
    makeUsage: 'Usage: zusantara make:job <name> [--schedule "0 7 * * *"]',
    template: {
      handler: "Do the work here: send emails, build reports, clean up data, etc.",
      retries: "Retried when it fails (default 3).",
      schedule: "Runs automatically on a cron schedule (minute hour day month weekday).",
    },
    enqueueHint: (name) => `Queue it: await enqueue("${name}", { ... })  (import { enqueue } from "zusantara")`,
  },
};
