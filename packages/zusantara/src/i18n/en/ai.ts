import type { Messages } from "../id/index.js";

export const ai: Messages["ai"] = {
  toolNames: {
    list_files: "List",
    read_file: "Read",
    search: "Search",
    list_routes: "Routes",
    write_file: "Write",
    edit_file: "Edit",
    delete_file: "Delete",
    run_check: "Check",
    run_command: "Run",
    database: "Database",
    zusantara: "Zusantara",
    install_package: "Install",
    ui_catalog: "UI catalog",
    view_page: "View",
    request_log: "Requests",
    dev_server: "Server",
  },
  summary: {
    lines: (n) => `${n} ${n === 1 ? "line" : "lines"}`,
    files: (n) => `${n} ${n === 1 ? "file" : "files"}`,
    routes: (n) => `${n} ${n === 1 ? "route" : "routes"}`,
    hits: (n) => `${n} ${n === 1 ? "match" : "matches"}`,
    view: (browser: boolean, device: string, issues: number, failed: number) =>
      `${browser ? "in the browser" : "text version"}${device ? ` · ${device}` : ""} · ${issues ? `${issues} layout ${issues === 1 ? "finding" : "findings"}` : "layout OK"}${failed ? ` · ${failed} failed ${failed === 1 ? "check" : "checks"}` : ""}`,
    catalog: (n: number) => `${n} components`,
    moreLines: (n) => `… (+${n} ${n === 1 ? "line" : "lines"})`,
  },
  tools: {
    urlRequired: "url is required, e.g. \"/notes\"",
    pathRequired: "path must be a string",
    pathInvalid: "invalid path",
    pathOutside: (input) => `paths outside the project folder are not allowed: ${input}`,
    pathSymlink: (input) => `path points outside the project through a symlink: ${input}`,
    writeDenied: (rel) => `the AI is not allowed to write to ${rel}`,
    fieldString: (key) => `field "${key}" must be a string`,
    truncated: (n) => `…(${n} characters cut)\n`,
    aborted: "Cancelled: the developer stopped this work.",
    declined: "The developer declined this action. Do not retry it; ask, or choose another approach.",
    folderNotFound: "folder not found",
    limitedFiles: "\n… (limited to 400 files)",
    empty: "(empty)",
    secretRead: (rel) => `${rel} contains secrets and must not be read by the AI`,
    fileNotFound: (rel) => `file not found: ${rel}`,
    fileTooBig: (size) => `file too large (${size} bytes)`,
    emptyQuery: "query must not be empty",
    noResults: "No results.",
    routesFailed: (reason) => `failed to load routes: ${reason}`,
    noRoutes: "(no routes yet)",
    isFolder: (rel) => `${rel} is a folder`,
    secretOverwrite: (rel) => `${rel} contains secrets and must not be overwritten by the AI`,
    unchanged: (rel) => `No changes: ${rel}`,
    writeSummary: (exists, rel, lines) => `${exists ? "Overwrite" : "Create"} ${rel} (${lines} ${lines === 1 ? "line" : "lines"})`,
    dryRunNotWritten: (rel) => `[dry-run] ${rel} not written`,
    written: (exists, rel) => `${exists ? "Overwritten" : "Created"}: ${rel}`,
    secretEdit: (rel) => `${rel} contains secrets and must not be edited by the AI`,
    oldTextEmpty: "old_text must not be empty",
    oldTextMissing: "old_text not found; read the file again and copy the text exactly",
    oldTextMany: (n) => `old_text appears ${n} times; add context to make it unique`,
    editSummary: (rel) => `Edit ${rel}`,
    dryRunNotEdited: (rel) => `[dry-run] ${rel} not edited`,
    edited: (rel) => `Edited: ${rel}`,
    deleteReason: "deletes a file",
    deleteSummary: (rel) => `Delete ${rel}`,
    dryRunNotDeleted: (rel) => `[dry-run] ${rel} not deleted`,
    deleted: (rel) => `Deleted: ${rel}`,
    checkInvalid: "check must be typecheck, test, or build",
    ok: "OK",
    failed: "FAILED",
    commandUnavailable: "run_command is not available in this session",
    commandRejected: (reason) => `Command rejected: ${reason}`,
    commandSummary: (line) => `Run: ${line}`,
    dryRunNotRun: (what) => `[dry-run] ${what} not run`,
    noOutput: "(no output)",
    dbActionInvalid: "action must be generate, migrate, or seed",
    dbReason: (migrate) => `${migrate ? "changes the structure of" : "adds data to"} the database (cannot be undone with undo)`,
    dbSummary: { generate: "Create a database migration", migrate: "Apply migrations to the database", seed: "Insert initial database data" },
    packageInvalid: (name) => `invalid package name: ${name}`,
    zusantaraSummary: (command) => `Run zusantara ${command}`,
    jobRunReason: "running a job can send emails or change data",
    unknownExample: (name: string, names: string) => `No such page example: ${name}. Options: ${names}.`,
    unknownComponent: (name: string, similar: string) => `No such UI kit component: ${name}.${similar ? ` Maybe: ${similar}.` : ""} Call ui_catalog without arguments to see them all.`,
    zusantaraArgInvalid: (arg) => `Invalid argument: ${arg}`,
    packageReason: "downloads and installs third-party code",
    dryRunNotInstalled: (name) => `[dry-run] ${name} not installed`,
    scriptMissing: (script) => `(no "${script}" script, skipped)`,
    stoppedByUser: "(stopped by the developer)",
    stoppedSignal: (signal) => `(stopped: ${signal})`,
    cliMissing: "Zusantara CLI not found",
    critical: {
      package: "changes the project's dependencies/scripts",
      config: "changes the Zusantara configuration",
      tsconfig: "changes the TypeScript configuration",
      github: "changes GitHub CI/workflows",
      gitignore: "changes the list of files ignored by git",
      env: "changes an environment file",
      migration: "edits a database migration by hand",
    },
  },
  command: {
    empty: "empty command",
    operators:
      "shell operators (|, &&, ;, >, <, $, %, `, !, ^, parentheses) are not supported. Run a single command without pipes/redirects; read its output file with read_file.",
    unclosedQuote: "unclosed quote",
    npmAccount: "touches npm account/credentials or publishes packages",
    pkgAccount: "touches account/credentials or publishes packages",
    gitAccount: "changes git configuration/credentials or pushes to a remote",
    ghAccount: "touches the GitHub account",
    forbidden: (program) => `"${program}" must not be run by the AI`,
    byName: "run programs by name (e.g. node, npx), not by path",
    denied: (cmd, reason) => `"${cmd}" rejected: ${reason}`,
    secretArg: (arg) => `argument "${arg}" refers to a secret file (.env/database); the AI must not read it`,
    outsideArg: (arg) => `argument "${arg}" refers to a path outside the project folder`,
    longRunning: "long-running commands (servers/watch) are not supported; use the dev_server tool",
    allowedBy: (entry) => `allowed by ai.allowedCommands ("${entry}")`,
    criticalReason: "runs a terminal command; changes it makes cannot be undone with undo",
    timedOut: (signal) => `(stopped: ${signal}, probably exceeded the time limit)`,
    exitCode: (code) => `(exit code ${code})`,
  },
  approval: {
    critical: "⚠ CRITICAL ACTION: ",
    needsApproval: (reason) => `Needs approval: ${reason}`,
    promptCritical: "[y] yes  [n] no",
    prompt: "[y] yes  [n] no  [a] approve all regular changes",
    yesWords: ["y", "yes", "ya"],
    noWords: ["n", "no", "t", "tidak", ""],
    allWords: ["a", "all", "s", "semua"],
    devServerReason: (cmd) => `runs ${cmd} in the background`,
    devServerStart: (cmd) => `Run ${cmd} in the background`,
    devServerRestart: "Restart the dev server",
  },
  agent: {
    noSummary: "The model did not return a summary.",
    summaryTag: "previous conversation summary",
    continueFromSummary: "Continue from this summary when I ask for something related.",
    understood: "Understood, I have the context of the previous conversation.",
    userStopped: "[The developer stopped this work. Wait for the next request.]",
    stopped: "OK, stopping.",
    refused: "The model refused this request.",
    truncated: "Your answer was cut off. Continue from where it stopped.",
    verifying: "Verifying the changes (typecheck & test)...",
    verified: "Verification passed.",
    stillFailing: "Verification still fails after several fix attempts.",
    fixing: (attempt, max) => `Verification failed, asking the AI to fix it (attempt ${attempt}/${max})...`,
    viewChecking: (attempt, max) => `Checking the changed pages (view_page desktop and mobile, attempt ${attempt}/${max})...`,
    viewRequest:
      'A page of the app changed. Before finishing, call view_page for every changed page with viewport "desktop" and "mobile" (expect: { noConsoleErrors: true, noLayoutIssues: true }), then fix what it finds.',
    viewFixRequest: (problems) =>
      `view_page still finds problems:\n${problems}\n\nFix the cause with the UI kit (no custom CSS), then check again with view_page desktop and mobile. If it cannot be fixed, report the findings as they are.`,
    viewStillFailing: (problems) => `The page checks (view_page) still find problems after two attempts: ${problems}`,
    fixRequest: (output) => `Automatic verification failed. Fix the cause:\n\n${output}`,
    inputTruncated: "The tool input was cut off (max_tokens). Split it into smaller steps/files.",
    stepLimit: (n) => `Reached the limit of ${n} steps.`,
    unknownTool: (name) => `Unknown tool: ${name}`,
    badInput: "Tool input must be a valid JSON object.",
    internalError: (reason) => `Internal error: ${reason}`,
    interrupted: "Stopped by the developer",
  },
  session: {
    saveFailed: (reason) => `The conversation could not be saved: ${reason}`,
    compacting: (k) => `The conversation is long (~${k}k tokens), summarizing first...`,
    compactFailed: (reason) => `Could not summarize the conversation: ${reason}`,
    result: {
      done: "✓ Done",
      incomplete: "… Not finished (step limit)",
      refused: "✗ Refused by the model",
      verification_failed: "✗ Verification failed",
      interrupted: "■ Stopped",
    },
    steps: (n, providers) => `· ${n} ${n === 1 ? "step" : "steps"}${providers ? ` · provider: ${providers}` : ""}`,
    changedFiles: (files) => `  Changed files: ${files}  (undo with: zusantara undo)`,
    invalidId: (id) => `Invalid session ID: ${id}`,
    untitledProject: "(unnamed)",
    noPackageJson: "(package.json not found)",
  },
  terminal: {
    thinking: (provider) => `… ${provider} thinking`,
    unavailable: (from, reason) => `  ✗ ${from} unavailable: ${reason}`,
    switching: (to) => `  → switching to ${to}...`,
  },
  omnirouteTips: (dashboard) => [
    `OmniRoute dashboard: ${dashboard} (add free providers in the Providers menu, e.g. OpenCode Free or Kiro)`,
    'The "auto" model works without an API key; OmniRoute picks a healthy free provider.',
  ],
  config: {
    unknownOrder: (name, choices) => `ZUSANTARA_AI_ORDER: unknown provider "${name}". Choices: ${choices}`,
    badMode: (mode) => `Invalid ai.mode: ${mode} (choose "ask" or "auto")`,
    badMaxSteps: (n) => `Invalid ai.maxSteps: ${n}`,
    badAllowed: 'ai.allowedCommands must be a list of strings, e.g. ["npm run lint"]',
    badCompactAt: (n) => `Invalid ai.compactAt: ${n}`,
    duplicate: (name) => `Duplicate AI provider name: ${name}`,
    badBaseUrl: (name) => `Provider ${name}: baseUrl must start with http:// or https://`,
    badType: (name, type) => `Provider ${name}: unknown type "${type}"`,
    noProviders: "No AI provider is configured yet",
    allUnavailable: (detail) => `All AI providers are unavailable:\n${detail}`,
  },
  providers: {
    claudeConnection: "cannot connect to the Claude API",
    badKey: "API key missing or invalid (401)",
    forbidden: "access denied (403)",
    modelMissing: "model not available for this account (404)",
    rateLimit: "quota/rate limit reached (429)",
    claudeServer: (status) => `Claude server error (${status})`,
    noCredit: "out of credits (402)",
    noKey: "API key not set (set ANTHROPIC_API_KEY in .env)",
    autoModel: "automatic model",
    connectFailed: (url) => `cannot connect to ${url}`,
    timeout: "timeout",
    creditLabel: "out of credits",
    quotaLabel: "quota/rate limit reached",
    unavailableLabel: "unavailable",
    noModels: "the server did not report any model",
    modelReady: (model) => `model ${model} ready`,
    modelNotListed: (model) => `connected, but model "${model}" is not in this account's model list`,
    serverError: (detail) => `server error: ${detail}`,
    noAnswer: "the response contains no answer",
    badJson: "the response is not valid JSON",
  },
  presets: {
    omniroute: "OmniRoute (free, a gateway to hundreds of AI providers)",
    claude: "Claude (Anthropic)",
    openai: "OpenAI",
    gemini: "Google Gemini",
    groq: "Groq",
    deepseek: "DeepSeek",
    openrouter: "OpenRouter (many models, one key)",
    ollama: "Ollama (local models, free & offline)",
  },
  setup: {
    number: "Number",
    enterIsOne: "(Enter = 1)",
    unknownPreset: (name, choices) => `Unknown provider: ${name}. Choices: ${choices}`,
    chooseProvider: "Choose an AI provider",
    alreadySet: "✓ already set",
    local: "local",
    cancelled: "Cancelled.",
    serverAddress: "Server address",
    enterIs: (value) => `(Enter = ${value})`,
    badAddress: "The address must start with http:// or https://",
    keepExisting: "Enter = keep the current one",
    optionalKey: "optional, Enter = skip",
    keyRequired: "An API key is required.",
    omnirouteStoppedForNow: "OmniRoute is stopped for now. `npx zusantara` will offer to run it in the background each time it opens.",
    checking: "Checking the connection & model list...",
    listNeedsKey: (model) => `The server is running; listing models needs an API key, so model "${model}" is used.`,
    connectFailed: (label, reason) => `✗ Could not connect to ${label}: ${reason}`,
    ensureRunning: (label) => `Make sure ${label} is running.`,
    notInstalled: "Not installed yet?",
    chooseModel: "Choose a model",
    newestOf: (shown, total) => ` (${shown} newest of ${total})`,
    current: "current",
    otherModel: "Another model…",
    otherModelHint: "type the model name yourself",
    modelName: "Model name",
    model: "Model",
    modelRequired: "A model is required.",
    modelNotListed: (model) => `⚠ Model "${model}" is not in this account's model list.`,
    saveAnyway: "Save anyway?",
    makePrimary: "Make it the primary provider (tried first)?",
    saved: "✓ Saved to .env",
    envNotIgnored: "⚠ .env is not in .gitignore yet. Add it so your API key is never committed!",
    configOverrides: "⚠ zusantara.config.mjs sets ai.providers itself, so the settings from .env are not used. Remove ai.providers for these settings to apply.",
    next: (status, tryIt) => `\nCheck the provider chain: ${status}\nTry it: ${tryIt}`,
    omnirouteMissing: (repo) => `OmniRoute is not installed. OmniRoute is free and needs no API key (${repo}).`,
    omnirouteNeedsNode: (version) => `OmniRoute needs Node.js 22.22+ or 24+ (you have ${version}). Update Node.js first.`,
    installNow: "Install it now with npm install -g omniroute?",
    installLater: "Install it later with:",
    installFailed: "Installation failed. Try it yourself: npm install -g omniroute (on Windows you may need an Administrator terminal).",
    installed: "✓ OmniRoute installed.",
    startingForTest: "Starting OmniRoute for a connection test...",
    cannotStart: "OmniRoute could not start. Latest logs:",
    running: (dashboard) => `✓ OmniRoute running · dashboard ${dashboard}`,
  },
};
